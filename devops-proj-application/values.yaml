image:
  repository: arpitamisal/devops-proj
  tag: d0d56cb
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

serviceAccount:
  enabled: false

autoscaling:
  enabled: false 

httpRoute:
  enabled: false

replicaCount: 1