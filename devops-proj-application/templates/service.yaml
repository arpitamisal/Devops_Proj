apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-proj-application.fullname" . }}
  labels:
    {{- include "devops-proj-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-proj-application.selectorLabels" . | nindent 4 }}
